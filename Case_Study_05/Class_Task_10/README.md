````
Variable- column
observation = rows
values = cells

pivot_wider = observation spread across two rows

pivot_longer() = adds rows decreases columns


separate = separates columns into rows, splits where it sees non-alphanumeric characters


unite() combines multiple columns into single column

complete() takes columns and finds all unqique columns, fills in NA's where neccessary

fill() takes columns and replaces missing value with most recent non-missing value


````

